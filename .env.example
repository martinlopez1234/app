# Plantilla. Copia a `.env` y rellena con tus valores (no subas `.env` a Git).

REACT_APP_SUPABASE_URL=https://TU-PROYECTO.supabase.co
REACT_APP_SUPABASE_ANON_KEY=TU_CLAVE_PUBLISHABLE_ANON
REACT_APP_SUPABASE_STORAGE_BUCKET=project-images

# Imágenes: el mantenedor sube al bucket configurado (por defecto `project-images`).
